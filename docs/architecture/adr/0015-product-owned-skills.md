# ADR 0015: product-owned declarative skills

JARVIS owns a metadata-first skill registry and progressive instruction loader.
Skills are orchestration data, not unrestricted plugins, and every tool action
uses the existing permission, approval, audit, and event pipeline.
