# Research setup

Offline research works with `LocalDocumentProvider` and configured document
roots. Browser and SearXNG integrations are optional injected adapters and are
not runtime dependencies. Keep source roots bounded and treat retrieved text
as untrusted evidence data.
