# Browser acceptance

The deterministic local browser controller is covered by automated tests.
Live Playwright acceptance requires an existing Node/Playwright installation,
an explicit target URL, navigation/read/extraction checks, timeout and
disconnect recovery, and proof that mutations remain approval-gated.

No Node or Playwright executable/package was available in the Phase 06 local
audit. Do not install it or claim live browser acceptance as part of this
phase.
