# Perception setup

Inject a screen provider when a local UIAutomation, browser DOM, or other
approved capture adapter is available. The service is on-demand only. Enable
OCR or local vision through a provider with an explicit capability; the
default runtime reports them deferred and retains no raw frame.
