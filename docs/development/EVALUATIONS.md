# Evaluations

Create deterministic cases with explicit expected values. Keep fixtures local,
small, and reproducible. A passing suite followed by a failing suite is a
regression and is emitted as `evaluation.regression_detected`. Store only
structured results and evidence, not model scratch state or chain-of-thought.
